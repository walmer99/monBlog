{% extends 'base.html.twig' %}

{% block body %}

    <h1>{{ tag.name }}</h1>

    {% for article in articles %}

        {{ article.title }}

    {% else %}
        <li>No tags for this article</li>
    {% endfor %}

{% endblock %}